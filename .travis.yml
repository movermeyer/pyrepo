---
language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
  - "pypy"
# install dependencies
install:
  - pip install -r requirements.txt
  - pip install --editable .
# run tests
script: nosetests
